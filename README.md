# Line Atelier · v4 结构修形与清线

以 v4 `67e63ed4e020a7bbec6714e3b4767d89c70c53dc` 为基线，v5 只参考流程及区域叠层理念。保留 Canvas 笔刷、按弧长逐笔回放、手动输入、工程文件、参考图与浏览器 WebMCP。

执行规范见 [DRAWING_WORKFLOW.md](docs/DRAWING_WORKFLOW.md)，实际工具说明见 [agent-guide.md](dist/agent-guide.md)。

本轮已实施：经过点拟合与尖角、保留源几何、局部坐标及共享锚点编辑、可见区间裁切、对象区域与纯线稿遮挡、透明镜片保护、几何分辨率重绘、原始参考裁切／滤镜线稿／同步翻转、六个线稿子阶段、结构化复核及失效标记、候选预览与可恢复检查点。

参考提取仅在 `reference.js` 内产生观察图像，不导入绘画引擎。旧自动描摹／分区补色文件保留作历史对照，当前 app 不导入它们。画布几何由模型或手动输入决定。

## 本轮实际绘画

`dist/refine-study.line.json` 由模型通过浏览器 WebMCP 操作完成，包含 353 条记录。原 `dist/lineart-study.line.json` 未改。新文件保留原稿、小腿修正及脸手局部试片三个检查点。

- 比较两种小腿形状后修正画面左侧膝下宽度、膝踝过渡；共享锚点保持连接。
- 显式定义腿遮住尾巴，处理加宽后穿入腿部的后景线。
- 原脸、镜框和手的 32 条粗线留在独立层；新增 32 条经过点清线试画，再修镜桥接头和指尖。
- 仍处于第一大阶段的 `refine`，手掌／指根、刘海交接和全身线稿仍需继续。未开始上色，未宣称达到原视频的美术精度。

`studies/refine-execution-inputs.json` 保存主要模型修订输入，实际生效几何及检查点以工程为准。回放显示当前轨迹；检查点显示修改里程碑，不是模型推理录像。

## 开发与验证

静态入口 `dist/index.html`；Vite 仅用于开发预览。

- `node --test tests/model.test.mjs tests/engine.test.mjs tests/geometry.test.mjs`：25 项相关测试通过。覆盖几何 round-trip、共享节点、角点、裁切、区域依赖、复核失效、阶段约束、候选隔离、回放与缓存。
- `node scripts/build-standalone.mjs`：生成新的 `line-atelier-v4-refine-20260905.html`，内嵌当前工程和参考。
- 实际浏览器 WebMCP 与三栏复核已验证。353 条记录的工程切换图层 8 次：新增绘制单元 0、图层重建 0，缓存合成约 0.1–0.5 ms，仅代表本次环境。

限制：滤镜参考会混入阴影边；旧采样线不自动恢复贝塞尔原件；基础关系提示不能发现全部拓扑／人体问题；隐藏前景图层不会取消独立的物体遮挡关系。精细线稿质量仍须实际观察、修订与验收。
