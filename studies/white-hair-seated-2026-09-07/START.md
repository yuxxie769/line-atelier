# 白发坐姿参考临摹

参考：`C:/Users/xie/Downloads/3b978c885e5a5cf538cd28ca349ae5f0.jpg`（600 × 849）。

已完整读取正式规范 `docs/WORKFLOW_PRINCIPLES.md`，SHA-256：`029c6069f167480319bdb1daa559d5686e3f10d89eb1d7701590c48638bb6992`。

## 当前状态

2026-09-07 用户已授权继续，并要求旧 R3 工程单独备份。已保存 `backup/r3-original.line.json` 和 `backup/r3-reference.jpg`，工程副本 SHA-256 与原 R3 文件一致：`6ca3d7cb9add73c460638a87f50f459ff21947a132225328ecb9de99e598b84c`。

新作品已实际在最新源码工作台推进到首版上色稿：1200 × 1698、443 条笔迹、revision 45、播放完成。包含 24 条人体定位、111 条粗稿、29 条细化草稿、145 条独立清线，以及 134 条底色、阴影与高光。底稿保留并隐藏，当前可见层展示首版上色。参考图单独保存为 `reference.jpg`，不进入作品 PNG。

可编辑工程：`white-hair-seated.line.json`。当前作品预览：`current-drawing.png`。独立清线另存为 `clean-lineart.line.json` 与 `clean-lineart.png`。工程保留 8 个检查点：1A 人体定位、1B 完整粗稿、1C 结构初修、1D 细化草稿、1E 独立清线、1F 返修复核、全身底色、首版上色与整体复看。

页面与本地工程全部 443 条完整笔迹记录的 JSON FNV-1a 一致：`f91e2e96`。SHA-256：`29fc5e507d31503476f31546fe3c04156f412879b4f9c042d72cc18495f8eafe`。遮挡区域由同一批模型已画路径的采样点重建，坐标在 1e-9 像素精度下一致；末位浮点值存在不同运行时的计算差异。已实际查看本地渲染预览。核对明细见 `integrity.json`；这只证明保存完整性，不代表美术质量通过。

`geometry.json`、`clean-geometry.json`、`color-geometry-1.json` 和 `color-geometry-2.json` 均来自实际 WebMCP 导出的源几何。`save-colored.mjs` 使用项目自己的验证、导出和渲染模块保存当前版本，不从参考像素生成笔迹。`save-study.mjs` 与 `save-clean.mjs` 是较早里程碑的保存脚本，不能直接运行它们覆盖当前上色稿。初期 1C 工程另存为 `backup/initial-1c.line.json`。

实际 WebMCP 会话：`f37ea726-00d0-47b5-b8bf-69410d2819bf`，来源 `http://127.0.0.1:5173/`。作画、依据、对照图片引用和复核已由页面独立保存至该来源 IndexedDB。用 `paint_export_document` 的 `section:evidence` 与该 `sessionId` 可继续读取。页面仍保留本次作品，继续前先 `paint_get_state`，不要重新载入旧 R3。

清线经历了两轮实际审核及返修：脸颈穿发、辫尾穿袖、领巾和衣摆接头已修正；revision 26 完成全身、缩小翻转、局部、缺口扫描与泄漏预览复核。新增空底色层后 revision 27 重新确认画面不变，延续两轮清线通过记录，再依次铺发色、肤色、衣服、配饰、阴影与高光。上色后修正了刘海漏肤色及裙腿色区白缝，并降低丝袜阴影和腮红强度。

当前为首版上色稿，尚未宣称精细临摹终稿通过。最终实际复看记录为 `observation / needs-work`：衣褶比参考简化，丝袜与肤色的明暗过渡更偏平涂。后续细化优先处理这些差异；任何轮廓修订仍需按正式规范重新复核受影响项。页面仍以 `lineart_review` 保存线稿子阶段；最新笔迹的绘画阶段为 `highlights`，最终复看另存于 reviews。

## 参考观察与起稿安排

以下坐标为模型目视估计的原参考坐标，属于待落笔验证的定位建议，不是从像素自动提取的路径。拟采用 1200 × 1698 画布，正式落笔坐标乘 2。

- 整体外接范围约 x=14–580、y=9–826，保留顶部饰带、光环、左飘发梢和底部鞋的完整留白。
- 头颅约 x=249–414、y=74–244；下巴约 (336,250)，颈根约 (350,278)。眼线向画面右上倾，画面左眼被刘海遮住。
- 胸廓中心约 (346,337)，骨盆约 (308,452)。先建立胸廓和骨盆转向，再包覆水手领、裙和外套。
- 画面左侧屈臂：肩约 (281,282)，肘约 (272,371)，腕约 (287,278)，手抬至下巴旁。先确定上臂与前臂折返，再安排袖口与手指。
- 画面右侧下垂臂：肩约 (413,319)，肘约 (405,469)，腕约 (393,625)，手伸出袖口与脚踝附近接触。宽大衣袖不能代替手臂体积。
- 两腿都向画面左方屈膝。后方较直的小腿由膝部约 (170,489) 向踝部约 (174,742) 下伸，鞋延至约 (154,821)。前方交叠腿由膝部约 (196,532) 斜向右下到踝约 (367,668)，脚尖下垂至约 (392,749)。后续必须连同骨盆、大腿和双膝一起复看，避免只凭小腿轮廓定姿态。
- 冠发与两侧长发分开；画面左方发束飘至 x≈15，画面右侧发束沿肩袖向下至 y≈550。将穿插发束按实际前后拆成对象。
- 头饰为左上折叠飘带和倾斜椭圆光环，分别保留几何身份；衣物以领口、领结、裙摆、两袖及后摆分组。

以上为起稿时的观察记录。后续从当前 revision 45 的上色稿继续，不重复新建，不以起稿估计代替当前实际观察。每组落笔读取真实底稿 ID 和源坐标，复看渲染后修订。
