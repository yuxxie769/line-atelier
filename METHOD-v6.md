# Line Atelier v6：模型决定笔触，画板执行

这版将默认画板改为按区域和叠层绘画。参考图只放在独立窗口，不进入作品合成，也不再驱动颜色聚类、像素色岛或残差补点程序。

## 实际改了什么

- **完整线稿在先**：8 笔结构草稿、282 笔轮廓和内部线条之后，才允许铺色。模型实际查看全图与脸手局部，修改了 3 条曲线，再确认线稿。回放默认在第 290 个操作后暂停。
- **区域＋叠层**：尾巴、后发、皮肤、内搭、外套、前发、配饰、鞋履共 8 组。每组包含底色、阴影、修色，另有草稿、完整线稿、最后压线，共 27 层。底色是完整部件，阴影在独立层上覆盖并裁剪到底色。
- **明确的上色顺序**：45 次部件底色 → 69 次大暗面 → 顺形高光和局部修色 → 18 笔选择性压线、镜面与金属细节。共有 455 个操作，其中 329 笔连续笔触、126 次区域覆盖。区域覆盖就是填充工具，界面会如实显示，不伪装成小点笔刷。
- **连续笔触**：沿轨迹弧长延伸，支持笔压变化与柔边。新增曲线修改工具，可以按笔迹编号改线；只有受影响的图层重画。
- **显隐性能**：保存各层及部件组合成缓存。切换显示不再回放全部笔迹；测试浏览器的几次单层显隐为 6.1–15 ms，重画 0 笔。设备和画布尺寸会影响实际耗时。旧版兼容画板的显隐也改为复用已画表面。

## 这幅画的来源和边界

复用了你提供 HTML 的 45 个部件几何和主要可见外轮廓。模型补画了 103 条线稿曲线，修正 3 条错线，并增加阴影形、长高光、镜片与手部等细节；实际通过网页的 WebMCP 工具分阶段执行。这不是从空白独立构造每一条轮廓，也没有达到原视频的美术精度。帽子、发束及整体造型仍有简化，不能用笔数或流程正确来代替美术质量判断。

## 打开与继续画

1. 打开 `line-atelier-v6-manual.html`，直接看到这轮作品。点击「从头播放」查看完整过程；「下一步」会延伸一笔。在线稿暂停点检查后继续。
2. 点击「仅看线稿」检查上色前的造型；在「图层」中单独关闭某个部件的阴影或整组显隐。
3. 继续创作时，在支持 WebMCP 的宿主中打开网页，并让模型读参考图与画布快照，再提交小批次绘画指令。普通浏览器上传参考图只更换参考窗口，不会自行调用模型。
4. 「保存工程」保留笔迹、区域、图层与参考图；PNG 是当前可见合成图。附带的 `.line.json` 可以重新打开编辑。

## 模型工作方式

`get_canvas_state`、`get_reference_image` → `start_drawing` 或 `create_part_group` → `draw_paths` / `draw_strokes` → 实际查看 `get_canvas_snapshot` → `revise_strokes` 修线 → `approve_lineart` → `fill_regions` 铺底色 → `paint_shapes` 叠暗面 → `draw_paths` 修色与压线。

线稿确认记录的是模型或用户的观察，不是程序自动判断画得好。内置回放是已经保存的作品记录；实时模型调用是另一回事。完整工程中的 `sessionActions` 保存了这轮实际调用记录，`provenance` 标注了复用的底稿。

## 验证

检查了线稿前禁止上色、线稿暂停点、单笔推进、笔迹修改、部件组显隐、阴影层显隐与缓存耗时。旧引擎及兼容功能的 17 项测试通过，其中新增测试确认显隐不重载工程且不改变回放位置。
